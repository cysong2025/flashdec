name: quality

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  repository-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install test runner
        run: python -m pip install pytest
      - name: Check documentation links
        run: python scripts/check_docs.py
      - name: Check release artifacts
        run: python scripts/check_release.py --require-evidence
      - name: Compile Python sources
        run: python -m compileall -q flashdec tests benchmarks scripts
      - name: Run dependency-free tests
        run: >-
          python -m pytest -q
          tests/test_docs_check.py
          tests/test_release_check.py
          tests/test_scheduler.py
          tests/test_multi_layer_workload_benchmark.py
          tests/test_multi_layer_workload_summary.py
